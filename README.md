# Movie-Reviews
This program defines a simple Keras neural network for binary classification, then loads and performs basic data exploration on the IMDb movie review dataset, including review length analysis.
